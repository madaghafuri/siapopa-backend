import Modal, { ModalContent, ModalHeader } from '../modal';

export const ModalOpt = () => {
  return (
    <Modal>
      <ModalHeader>Create Modal</ModalHeader>
      <ModalContent>
        <form>
          <div>label</div>
        </form>
      </ModalContent>
    </Modal>
  );
};
